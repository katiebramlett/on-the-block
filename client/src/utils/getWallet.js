import Web3 from "web3";

// just have these as utility functi9ons that get called
// in useeffect and can be used to update state 
// eventually remember which users consented to have their wallets connected
// across website sessions so only approval happens once
function getWallet() {

}

export default getWallet;


// we can set up everything in App and just pass through props? 

// kind of confusing but could work I guess 

// can just set state through useffect 